name: Build Custom Arch ISO

on:
  push:
    branches: [main]

permissions:
  contents: write  # Required for tagging and releases

jobs:
  build-iso:
    runs-on: ubuntu-latest

    container:
      image: archlinux:latest
      options: --privileged  # Needed by mkarchiso to use systemd-nspawn

    steps:
      # Checkout the repository
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Install dependencies
      - name: Install dependencies
        run: |
          pacman -Syu --noconfirm
          pacman -S --noconfirm archiso git rsync base-devel

      # Setup Archiso releng profile
      - name: Setup Archiso releng profile
        run: |
          mkdir -p iso_build
          cp -r /usr/share/archiso/configs/releng iso_build/
          cd iso_build/releng
          rm -rf airootfs/etc/skel || true

      # Copy dotfiles and scripts
      - name: Copy dotfiles and scripts
        run: |
          mkdir -p iso_build/releng/airootfs/etc/skel/.config
          rsync -av .config/ iso_build/releng/airootfs/etc/skel/.config/ || true
          mkdir -p iso_build/releng/airootfs/usr/local/bin

      # Copy archinstall config and README
      - name: Copy archinstall config
        run: |
          cp archinstall.yaml iso_build/releng/airootfs/archinstall.yaml || true
          cp README.md iso_build/releng/airootfs/README.md || true

      # Build the ISO
      - name: Build ISO
        run: |
          cd iso_build/releng
          mkdir -p work out
          export MKARCHISO_NSPAWN=0
          mkarchiso -v -w work -o out .

      # Create incrementing tag (V1, V2, etc.)
      - name: Create new Git tag
        id: tag
        run: |
          git fetch --tags
          LATEST_TAG=$(git tag --list "V*" | sort -V | tail -n 1)
          if [ -z "$LATEST_TAG" ]; then
            NEXT_NUM=1
          else
            NEXT_NUM=$(( ${LATEST_TAG#V} + 1 ))
          fi
          NEW_TAG="V$NEXT_NUM"
          echo "Creating new tag: $NEW_TAG"
          git tag "$NEW_TAG"
          git push origin "$NEW_TAG"
          echo "tag=$NEW_TAG" >> $GITHUB_OUTPUT

      # Create GitHub Release
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          name: "Arch ISO ${{ steps.tag.outputs.tag }}"
          body: "Automated build of custom Arch ISO."
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Upload ISO to Release
      - name: Upload ISO to Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          files: iso_build/releng/out/*.iso
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
