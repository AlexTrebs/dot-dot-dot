# Prefer Wayland for GTK, Qt, and Electron apps
GDK_BACKEND=wayland
QT_QPA_PLATFORM=wayland
CLUTTER_BACKEND=wayland

# Ensure Electron apps (like Discord, VSCode, etc.) know they're on Wayland
XDG_SESSION_TYPE=wayland
XDG_CURRENT_DESKTOP=Hyprland
